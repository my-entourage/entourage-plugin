# .env.local - Local environment variables (NEVER commit this file)
#
# Copy this file to .env.local in your project root.
# These tokens are only needed as fallbacks when MCP servers or CLI tools
# are not available.
#
# Authentication hierarchy (preferred to fallback):
#   1. MCP servers - Linear MCP, Supabase MCP (OAuth via browser)
#   2. CLI tools - gh CLI, gcloud CLI (credentials in system keychain)
#   3. Environment variables - This file (gitignored, never committed)

# Linear API Token (fallback when Linear MCP is unavailable)
# Generate at: https://linear.app/settings/api
LINEAR_API_TOKEN=lin_api_xxxxxxxxxxxx

# GitHub Token (fallback when gh CLI is unavailable)
# Generate at: https://github.com/settings/tokens
# Required scopes: repo, read:org, workflow
GITHUB_TOKEN=ghp_xxxxxxxxxxxx
